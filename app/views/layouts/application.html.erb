<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://kit.fontawesome.com/c47b0638d0.js" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header>
    <nav class="navbar navbar-expand-lg navbar navbar-dark" style="background-color: #191970;">
     <div class="container"><a class="navbar-brand" href="/">Bookers</a>
      <%#ハンバーガーメニュー後程%>
       <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
         <%= render 'layouts/user_header' %>
        <% else %>
         <%= render 'layouts/logout_header' %>
        <% end %>
       </ul>
     </div>
    </nav>
   </header>
   <main>
    <p class="notice"><%= notice %></p>
    <%= yield %>
   </main>
   <footer>
     <%= render 'layouts/footer' %>
   </footer>
  </body>
</html>
